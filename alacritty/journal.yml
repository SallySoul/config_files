window:
  title: Captain's Log 

font:
  size: 16
  # Normal (roman) font face
  normal:
    # Font family
    #
    # Default:
    #   - (macOS) Menlo
    #   - (Linux/BSD) monospace
    #   - (Windows) Consolas
    family: Cloister Black

    # The `style` can be specified to pick a specific face.
    #style: Regular

import:
  - /Users/russell/projects/config_files/alacritty/dracula.yml
